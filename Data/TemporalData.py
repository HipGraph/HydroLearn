import numpy as np
import os
from progressbar import ProgressBar
from Container import Container
import Utility as util


class TemporalData(Container):

    def __init__(self, var):
        raise NotImplementedError("Temporal data not yet supported.")
